extends layout

block content 
  .delete-container
    .delete-form
      h1.delete-title #{title}: #{copy.game.title}
      p Do you really want to delete this Game copy?
      p= copy.game.title 
      p= copy.price 
      p= copy.format 
      p= copy.condition
      .delete-confirm-form
        form(action="" method='POST')
          input#copyid(type='hidden' name='copyid' required='true' value=copy.id)
          button.button-delete-confirm(type='submit') Delete